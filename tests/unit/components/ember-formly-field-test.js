import { moduleForComponent, test } from 'ember-qunit';

moduleForComponent('ember-formly-field', 'Unit | Component | ember formly field', {
  // needs: ['component:ember-formly-component-unknown'],
  unit: true
});

// test('it renders', function(assert) {
//   let component = this.subject();
  
//   // this.render();

//   // assert.equal(this.$().text().trim(), '');
// });
